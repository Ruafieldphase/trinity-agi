# LUON / ELO Production Bundle (MVP)

This bundle contains:
- ELO schema, converter, metrics
- Canary simulator + dispatcher (dry-run aware)
- Ops runbooks and quickstart

See `runbooks/OPS_OnePager.md` for day-2 ops. Use `ops/Makefile` targets to run the demo.

# API Whitelist Policy (stub v0.1)
#
# 문서 참조 복구용 스텁.
# 실제 네트워크 허용/차단은 실행 레이어에서 강제되어야 한다.

version: "api_whitelist_policy_stub_v0_1"
generated_at: "2025-12-19"

network:
  default: "deny"
  allow:
    - host: "127.0.0.1"
      ports: [8091, 8093]
      note: "local services"


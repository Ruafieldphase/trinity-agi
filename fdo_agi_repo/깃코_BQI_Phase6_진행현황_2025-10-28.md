# BQI Phase 6 진행현황

**날짜**: 2025-10-28  
**목표**: 비노체 페르소나 디지털 트윈 → 80% 작업 자동화

## 현재 진행 상태

### Phase 6a: 패턴 학습 ✅ 완료

#### 구현 내용

**파일**: `scripts/rune/binoche_persona_learner.py` (407 lines)

**기능**:

1. **Ledger 분석**: 5636개 이벤트에서 269개 task 추출
2. **의사결정 패턴 분류**: approve/revise/reject 자동 분류
3. **BQI-Decision 상관관계**: BQI 패턴별 승인 확률 계산
4. **기술 선호도 추출**: task description + tool_call에서 tech stack 추출
5. **작업 스타일 분석**: quality_threshold, documentation/TDD 선호도
6. **자동화 규칙 생성**: if-then 규칙 (auto-approve/revise/reject/ask_user)

#### 첫 실행 결과 (2025-10-28 14:51)

```bash
[Binoche] Loading resonance ledger...
[Binoche] Loaded 5636 events
[Binoche] Grouping events by task...
[Binoche] Analyzed 269 tasks

[Binoche] Analyzing decision patterns...
[Binoche] Decision Patterns:
  - Approve: 0 cases
  - Revise:  1 cases
  - Reject:  1 cases
[Binoche] Distribution:
  - Approve: 0.0%
  - Revise:  50.0%
  - Reject:  50.0%

[Binoche] Analyzing BQI-decision correlation...
[Binoche] Found 1 BQI patterns
[Binoche] Top BQI Patterns:
  p1_e:keywords_r:unknown: approve=0.00, samples=2

[Binoche] Analyzing tech preferences...
[Binoche] Tech Stack:
  (empty - 데이터 부족)

[Binoche] Analyzing work style...
[Binoche] Work Style:
  - Quality Threshold: 0.00

[Binoche] Generating persona rules...
[Binoche] Generated 2 rules

[Binoche] Persona model saved to: D:\nas_backup\fdo_agi_repo\outputs\binoche_persona.json
[Binoche] Model summary:
  - Tasks analyzed: 269
  - BQI patterns: 1
  - Decision rules: 2
  - Top tech: []
```

#### 데이터 스파스 원인

**정상 현상**: Phase 5에서 `pipeline.py`에 BQI 추가한 지 얼마 안 됨

```python
# pipeline.py line 137 (Phase 5에서 추가)
"bqi_coord": bqi_coord.to_dict()
```

**해결책**: 1-2주 task 실행 → ledger에 BQI 누적 → 재학습 시 풍부한 패턴 생성

#### 출력 파일

**binoche_persona.json** 구조:

```json
{
  "version": "1.0.0",
  "created_at": "2025-10-28T14:51:49.149919",
  "stats": {
    "total_tasks": 269,
    "total_decisions": 2,
    "approve_rate": 0.0,
    "revise_rate": 0.5,
    "reject_rate": 0.5
  },
  "decision_patterns": {
    "approve": [],
    "revise": [
      {
        "bqi": {"priority": 1, "emotion": ["keywords"], "rhythm": "unknown"},
        "quality": 0.4,
        "confidence": null
      }
    ],
    "reject": [
      {
        "bqi": {"priority": 1, "emotion": ["keywords"], "rhythm": "unknown"},
        "quality": 0.4,
        "confidence": null
      }
    ]
  },
  "bqi_probabilities": {
    "p1_e:keywords_r:unknown": {
      "approve_prob": 0.0,
      "revise_prob": 0.5,
      "reject_prob": 0.5,
      "sample_count": 2
    }
  },
  "tech_preferences": {
    "tech_stack": {},
    "tools_used": {}
  },
  "work_style": {
    "quality_threshold": 0.0,
    "prefers_documentation": false,
    "prefers_tdd": false,
    "communication_style": "balanced"
  },
  "rules": [
    {
      "condition": "quality < 0.5",
      "action": "reject",
      "confidence": 0.9,
      "reasoning": "품질이 최소 기준 미달"
    },
    {
      "condition": "confidence < 0.6",
      "action": "ask_user",
      "confidence": 1.0,
      "reasoning": "과거 사례 불충분, 실제 비노체 확인 필요"
    }
  ]
}
```

#### VS Code Tasks 추가

**tasks.json** (lines 3287-3330):

```json
{
  "label": "🤖 Phase 6: Learn Binoche Persona",
  "type": "shell",
  "command": "powershell",
  "args": [
    "-NoProfile", "-ExecutionPolicy", "Bypass", "-Command",
    "cd D:\\nas_backup\\fdo_agi_repo; if (Test-Path .venv\\Scripts\\python.exe) { .venv\\Scripts\\python.exe scripts\\rune\\binoche_persona_learner.py } else { python scripts\\rune\\binoche_persona_learner.py }"
  ],
  "group": "test"
},
{
  "label": "🤖 Phase 6: Open Binoche Persona Model",
  "type": "shell",
  "command": "powershell",
  "args": [
    "-NoProfile", "-ExecutionPolicy", "Bypass", "-Command",
    "code D:\\nas_backup\\fdo_agi_repo\\outputs\\binoche_persona.json"
  ],
  "group": "none"
}
```

## 다음 단계

### Phase 6b: 자동 의사결정 모듈 (2주)

**목표**: `BinochePersona` 클래스 구현

**파일**: `fdo_agi_repo/orchestrator/binoche_persona.py`

**핵심 기능**:

```python
class BinochePersona:
    def __init__(self, model_path):
        """페르소나 모델 로드"""
        self.model = json.load(open(model_path))
        self.decision_patterns = self.model["decision_patterns"]
        self.bqi_probabilities = self.model["bqi_probabilities"]
        
    def review_proposal(self, proposal, context):
        """AGI 제안 자동 리뷰"""
        
        # 1. 유사 과거 사례 검색
        similar_cases = self.find_similar_decisions(
            proposal=proposal,
            bqi=context["bqi"],
            top_k=5
        )
        
        # 2. 승인 확률 계산
        approval_prob = np.mean([c["approved"] for c in similar_cases])
        
        # 3. 의사결정
        if approval_prob > 0.8:
            return Decision(
                action="approve",
                confidence=approval_prob,
                reasoning=f"과거 유사 상황 {len(similar_cases)}건 중 {approval_prob*100:.0f}% 승인"
            )
        elif approval_prob < 0.3:
            return Decision(
                action="reject",
                confidence=1 - approval_prob,
                reasoning="과거 유사 상황에서 거절 빈도 높음",
                suggestion=self.generate_alternative()
            )
        else:
            return Decision(
                action="ask_user",
                confidence=0.5,
                reasoning="과거 사례 불충분, 실제 비노체 확인 필요"
            )
    
    def find_similar_decisions(self, proposal, bqi, top_k=5):
        """유사 의사결정 검색"""
        
        # BQI 패턴 매칭
        bqi_key = f"p{bqi['priority']}_e:{','.join(bqi['emotion'])}_r:{bqi['rhythm']}"
        
        if bqi_key in self.bqi_probabilities:
            # 동일 BQI 패턴의 과거 사례
            return self.decision_patterns[bqi_key]
        else:
            # 유사 BQI 패턴 검색 (priority, emotion 유사도 기반)
            return self._fuzzy_match_bqi(bqi, top_k)
    
    def generate_alternative(self):
        """거절 시 대안 제시"""
        return {
            "suggestion": "품질 향상 후 재시도",
            "checklist": [
                "테스트 커버리지 80% 이상",
                "문서화 완료",
                "코드 리뷰 통과"
            ]
        }
```

**테스트 시나리오**:

```python
# 테스트 1: 자동 승인 (높은 확률)
proposal = "FastAPI REST API + pytest 테스트"
context = {
    "bqi": {"priority": 3, "emotion": ["confidence"], "rhythm": "implementation"},
    "quality": 0.9
}
decision = persona.review_proposal(proposal, context)
assert decision.action == "approve"
assert decision.confidence > 0.8

# 테스트 2: 자동 거절 (낮은 확률)
proposal = "보안 검증 없는 인증 시스템"
context = {
    "bqi": {"priority": 4, "emotion": ["concern"], "rhythm": "security"},
    "quality": 0.4
}
decision = persona.review_proposal(proposal, context)
assert decision.action == "reject"
assert decision.suggestion is not None

# 테스트 3: Ask User (불확실)
proposal = "새로운 ML 모델 실험"
context = {
    "bqi": {"priority": 2, "emotion": ["curiosity"], "rhythm": "exploration"},
    "quality": 0.7
}
decision = persona.review_proposal(proposal, context)
assert decision.action == "ask_user"
```

### Phase 6c: AGI 팀 협업 (3주)

**목표**: 멀티 에이전트 시스템 구축

**파일**: `fdo_agi_repo/orchestrator/multi_agent_collaboration.py`

**아키텍처**:

```python
class AGITeam:
    def __init__(self, binoche_persona):
        self.persona = binoche_persona  # 팀 리더
        self.agents = {
            "architect": ArchitectAgent(),
            "developer": DeveloperAgent(),
            "reviewer": ReviewerAgent(),
            "documenter": DocumenterAgent()
        }
    
    def execute_goal(self, goal):
        """목표 자동 실행"""
        
        # 1. 비노체 페르소나가 계획 수립
        plan = self.persona.create_plan(goal)
        
        # 2. 서브태스크 자동 분배
        subtasks = self.persona.auto_delegate_subtasks(plan)
        
        # 3. 병렬 실행
        results = []
        for subtask in subtasks:
            agent = self.agents[subtask["type"]]
            result = agent.execute(subtask)
            
            # 4. 비노체 페르소나가 자동 리뷰
            decision = self.persona.review_proposal(result, subtask["context"])
            
            if decision.action == "approve":
                results.append(result)
            elif decision.action == "revise":
                # 자동 수정 요청
                result = agent.revise(result, decision.reasoning)
                results.append(result)
            else:  # ask_user
                # 사용자 확인 필요
                user_decision = self.ask_user(result, decision)
                if user_decision == "approve":
                    results.append(result)
        
        return results

class ArchitectAgent:
    def execute(self, subtask):
        """설계 문서 자동 생성"""
        return {"type": "design", "content": "..."}

class DeveloperAgent:
    def execute(self, subtask):
        """코드 자동 구현"""
        return {"type": "code", "content": "..."}

class ReviewerAgent:
    def execute(self, subtask):
        """코드 자동 리뷰"""
        return {"type": "review", "content": "..."}

class DocumenterAgent:
    def execute(self, subtask):
        """문서 자동 업데이트"""
        return {"type": "docs", "content": "..."}
```

**실행 예시**:

```python
team = AGITeam(binoche_persona)

# 사용자 요청
goal = "Phase 7: RAG 검색 정확도 개선 (BM25 + Vector 하이브리드)"

# 자동 실행
results = team.execute_goal(goal)

# 출력:
# [Architect] 설계 문서 생성 완료 → 비노체 페르소나 자동 승인 (confidence: 0.92)
# [Developer] 코드 구현 완료 → 비노체 페르소나 자동 승인 (confidence: 0.85)
# [Reviewer] 코드 리뷰 완료 → 품질 95%, 테스트 통과
# [Documenter] 문서 업데이트 완료 → 비노체 페르소나 자동 승인 (confidence: 0.88)
# 
# 결과: 4/4 subtasks 자동 승인 (사용자 개입 0%)
```

### Phase 6d: Pipeline 통합 (4주)

**목표**: `Pipeline.run()` 수정하여 비노체 페르소나 자동 개입

**파일**: `fdo_agi_repo/orchestrator/pipeline.py`

**변경 사항**:

```python
class Pipeline:
    def __init__(self):
        self.persona = BinochePersona.load('outputs/binoche_persona.json')
        self.team = AGITeam(self.persona)
    
    def run(self, user_input, bqi_coord):
        """자동화된 파이프라인"""
        
        # 기존 로직...
        
        # 새로 추가: 비노체 페르소나 자동 리뷰
        proposal = {
            "goal": user_input,
            "plan": plan,
            "code": code,
            "tests": tests
        }
        
        decision = self.persona.review_proposal(proposal, {
            "bqi": bqi_coord.to_dict(),
            "quality": quality,
            "confidence": confidence
        })
        
        if decision.action == "approve":
            # 자동 승인 → 즉시 실행
            logger.info(f"[Binoche] Auto-approved (confidence: {decision.confidence:.2f})")
            return self._execute_immediately(proposal)
        
        elif decision.action == "reject":
            # 자동 거절 → 대안 제시
            logger.warning(f"[Binoche] Auto-rejected: {decision.reasoning}")
            alternative = decision.suggestion
            return self._propose_alternative(alternative)
        
        else:  # ask_user
            # 사용자 확인 필요
            logger.info(f"[Binoche] Asking user: {decision.reasoning}")
            return self._ask_user_confirmation(proposal, decision)
```

## 성공 메트릭

### 자동화율 (목표: 80%)

| 카테고리 | 목표 | 현재 | 측정 방법 |
|---------|------|------|-----------|
| 자동 승인률 | 65% | 0% (데이터 부족) | 승인 task / 전체 task |
| 자동 거절률 | 15% | 0% | 거절 task / 전체 task |
| Ask User 비율 | 20% | 100% | 확인 요청 / 전체 task |

### 정확도 (목표: 90%)

| 메트릭 | 목표 | 현재 | 측정 방법 |
|--------|------|------|-----------|
| 자동 승인 정확도 | 90% | N/A | 자동 승인 후 실제 승인 비율 |
| 자동 거절 정확도 | 85% | N/A | 자동 거절 후 실제 거절 비율 |
| False Positive | < 10% | N/A | 잘못된 자동 승인 비율 |

### 효율성

| 메트릭 | 목표 | 현재 |
|--------|------|------|
| 평균 응답 시간 | < 10초 | N/A |
| 24시간 자율 작동 | 100% | 0% |
| 야간 작업 처리 | 10+ tasks/night | 0 |

## 학습 데이터 누적 계획

### 1주차 (2025-10-28 ~ 11-03)

- **목표**: 최소 50개 task 실행 (BQI 포함)
- **방법**: 일상 작업 시 pipeline 사용
- **예상 패턴**: p3_e:focus_r:implementation (가장 빈번)

### 2주차 (2025-11-04 ~ 11-10)

- **목표**: 100개 task 누적
- **재학습**: Phase 6a learner 재실행
- **예상 결과**:
  - 5-10개 BQI 패턴
  - 10-20개 자동화 규칙
  - 기술 선호도 확인 (Python, PowerShell)

### 4주차 (2025-11-11 ~ 11-24)

- **목표**: 200개 task 누적
- **Phase 6b 구현**: 자동 의사결정 모듈
- **A/B 테스트**:
  - 50% 자동화 파이프라인
  - 50% 기존 파이프라인
  - 정확도/효율성 비교

## 문서

- [설계 문서](./깃코_BQI_Phase6_비노체페르소나_설계_2025-10-28.md)
- [진행현황 문서](./깃코_BQI_Phase6_진행현황_2025-10-28.md) (이 문서)

## 실행 방법

### Phase 6a: 패턴 학습 (현재 가능)

```powershell
# VS Code Task 사용
# Ctrl+Shift+P → "Tasks: Run Task" → "🤖 Phase 6: Learn Binoche Persona"

# 또는 PowerShell 직접 실행
cd d:\nas_backup\fdo_agi_repo
.\.venv\Scripts\python.exe scripts\rune\binoche_persona_learner.py
```

### 결과 확인

```powershell
# VS Code Task 사용
# Ctrl+Shift+P → "Tasks: Run Task" → "🤖 Phase 6: Open Binoche Persona Model"

# 또는 직접 열기
code d:\nas_backup\fdo_agi_repo\outputs\binoche_persona.json
```

### 대시보드 통합 (TODO)

```powershell
# quick_status.ps1에 추가 예정
$personaJson = Get-Content "outputs\binoche_persona.json" | ConvertFrom-Json
Write-Host "[Binoche Persona]" -ForegroundColor Cyan
Write-Host "  Tasks Analyzed: $($personaJson.stats.total_tasks)"
Write-Host "  BQI Patterns: $(($personaJson.bqi_probabilities | Get-Member -MemberType NoteProperty).Count)"
Write-Host "  Decision Rules: $($personaJson.rules.Count)"
Write-Host "  Auto-Approve Rate: $([math]::Round($personaJson.stats.approve_rate * 100, 1))%"
```

## 다음 세션 인계사항

1. **Phase 6a 완료** ✅
   - `binoche_persona_learner.py` 구현 (407 lines)
   - 첫 실행 완료 (269 tasks, 2 decisions)
   - VS Code tasks 추가 완료

2. **데이터 수집 중** 🔄
   - 현재: 2개 decision (sparse)
   - 필요: 50-100개 decision
   - 방법: 일상 작업 시 pipeline 사용 → 자동 누적

3. **다음 작업** 📝
   - Phase 6b 설계 시작 (2주 후)
   - 대시보드 통합 (persona 메트릭 표시)
   - 자동화 통합 (`run_bqi_learner.ps1`에 추가)

---

**Phase 6는 BQI의 최종 진화입니다. 비노체 페르소나는 AGI 팀을 지휘하여 80% 작업을 자동화하고, 사용자 개입을 최소화합니다.** 🚀

# 2025-10-26 저품질 평가 이벤트 진단

최근 6시간(`ledger_summary_latest.json`) 기준으로 `eval.quality < 0.65`인 이벤트가 22건 확인되었습니다. 모든 항목의 품질 점수는 0.4이며 `evidence_ok = false` 상태입니다.

## 공통 패턴
- **task_id prefix**: `integration_test_` 또는 `low_confidence_test_` (일부 `temp_low_conf_`).  
- **발생 시각**: 2025-10-26 02:32–03:12(UTC) 사이의 일괄 실행 구간.  
- **노트**: `eval.notes = "auto"` – 자동 평가 흐름에서 증거 부족으로 점수 하락.  
- **원인**: 회귀 검증 스크립트(`scripts/test_phase5_integration.py`)가 반복 실행되면서,
  - 시나리오 1은 정상 메타인지+학습 플로우를 확인하지만, 현재 환경에서는 외부 검색이 연결되지 않아 인용(citation)이 0건 → 품질 휴리스틱(0.4 + 0.1 * citations)이 최저점(0.4)을 반환.
  - 시나리오 2는 `WEBSEARCH_DISABLE=1` 강제 설정으로 저신뢰 경고를 의도적으로 유도 → 역시 증거가 없어 평가에서 0.4로 기록.

## 영향 평가
- **평균 품질**: 0.611 (6시간 윈도우). 운영 작업(task_id prefix가 다른 항목)은 정상적으로 0.8 이상을 유지.  
- **경고 지표**: `low_confidence_warning` 4건 모두 시나리오 2 회귀 테스트에서 발생 → 시스템 문제라기보다 테스트 목적.  
- **완료율**: 100% (실패 태스크 없음).  

## 권고 사항
1. **테스트 러너 주석**  
   - 테스트를 반복 실행할 때는 `docs/TESTING.md` 또는 해당 스크립트 상단에 “외부 검색 비활성화 상태에서는 품질이 0.4로 떨어지며, 운영 지표 해석 시 제외할 것”이라는 주석을 추가하는 것을 권장.
2. **품질 대시보드 필터링**  
   - 메타 대시보드에서 `task_id` 혹은 `goal`을 기준으로 테스트 이벤트를 제외한 운영 품질 지표를 파생 계산 (예: `where task_id not like 'integration_test_%'`). 필요 시 자동 리포트 스크립트에 필터 옵션 추가.
3. **증거 수집 시뮬레이션 옵션 검토**  
   - 향후 품질 휴리스틱 왜곡을 줄이기 위해, 테스트 모드에서만 최소 한 건의 더미 citation을 기록하도록 하는 방안을 고려 (실제 운영과의 분리 전제 필요).

이번 저품질 이벤트는 회귀 테스트 목적에 따른 정상 동작으로 판단되며, 추가 시정 조치는 불필요합니다. 다만 상기 두 번째 권고(지표 분리)는 누적 데이터 분석을 수월하게 하기 위해 수행하는 것을 추천합니다.

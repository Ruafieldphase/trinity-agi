# 🎯 Gitko Extension - 실전 사용 예제

**v0.3.1 기준** | **최종 업데이트**: 2025-11-14

---

## 📖 목차

1. [빠른 시작](#빠른-시작)
2. [Language Model Tools (자동 호출)](#language-model-tools)
3. [Chat Participant (수동 호출)](#chat-participant)
4. [모니터링 대시보드](#모니터링-대시보드)
5. [개발자 도구](#개발자-도구)
6. [실전 시나리오](#실전-시나리오)

---

## 🚀 빠른 시작

### 1단계: Extension 실행

```powershell
# 프로젝트 폴더에서
.\test-extension.ps1  # 자동 검증
# 또는
F5  # Extension Development Host 실행
```

### 2단계: 첫 명령어 실행

**Copilot Chat**에서:
```
이 코드를 리팩토링해줘
```

→ Sian Agent가 자동으로 실행됩니다!

---

## 🤖 Language Model Tools (자동 호출)

### Sian Agent (코드 개선)

**자동 호출 키워드**: 리팩토링, 개선, 최적화, 현대화, 클린코드

#### 예제 1: 함수 리팩토링

```
사용자: 이 함수가 너무 길어. 읽기 쉽게 만들어줘

Copilot: (Sian 자동 호출)
✅ 함수를 3개로 분리했습니다
- extractData(): 데이터 추출
- validateData(): 검증
- processData(): 처리
```

#### 예제 2: 성능 최적화

```
사용자: 이 루프가 느린데 최적화 방법 있을까?

Copilot: (Sian 자동 호출)
✅ O(n²) → O(n) 개선 제안
- Map 사용으로 조회 최적화
- 불필요한 중복 제거
```

#### 예제 3: 모던 코드로 변환

```
사용자: 이 코드를 ES6+ 스타일로 바꿔줘

Copilot: (Sian 자동 호출)
✅ 현대화 완료
- var → const/let
- function → arrow function
- Promise 체이닝 → async/await
```

---

### Lubit Agent (코드 리뷰)

**자동 호출 키워드**: 리뷰, 검토, 보안, 버그, 취약점, 안전

#### 예제 1: 보안 검토

```
사용자: SQL 인젝션 위험이 있는지 확인해줘

Copilot: (Lubit 자동 호출)
⚠️ 보안 문제 발견!
1. SQL 쿼리에 직접 변수 삽입
   → Prepared Statement 사용 권장
2. 입력 검증 부족
   → 파라미터 검증 추가 필요
```

#### 예제 2: 버그 탐지

```
사용자: 이 코드에 버그 있을까?

Copilot: (Lubit 자동 호출)
🐛 잠재적 버그 발견
1. null 체크 누락 (line 15)
2. 비동기 처리 에러 핸들링 없음
3. 메모리 누수 가능성 (이벤트 리스너)
```

#### 예제 3: 베스트 프랙티스 검증

```
사용자: 이 React 컴포넌트가 표준을 따르는지 봐줘

Copilot: (Lubit 자동 호출)
✅ 전반적으로 양호
⚠️ 개선 사항:
- useEffect 의존성 배열 확인
- PropTypes 또는 TypeScript 권장
- key prop 추가 필요
```

---

### Gitko Orchestrator (복합 작업)

**자동 호출 키워드**: 전체, 프로젝트, 종합, 복합, 동시에

#### 예제 1: 프로젝트 전체 개선

```
사용자: 이 프로젝트 전체를 점검하고 개선해줘

Copilot: (Gitko 자동 호출)
🎭 Gitko가 Sian + Lubit 조율 중...

📊 분석 결과:
- Sian: 23개 파일 리팩토링 제안
- Lubit: 5개 보안 이슈 발견
- 총 38개 개선사항 도출
```

#### 예제 2: 코드 + 문서 동시 개선

```
사용자: 코드 개선하고 문서도 업데이트해줘

Copilot: (Gitko 자동 호출)
🎯 복합 작업 실행 중...
✅ Sian: 코드 리팩토링 완료
✅ 문서 생성: README.md 업데이트
✅ JSDoc 주석 추가
```

---

## 💬 Chat Participant (수동 호출)

### 기본 사용법

```
@gitko [명령어] [메시지]
```

### 사용 가능한 명령어

#### `/help` - 도움말

```
@gitko /help

출력:
🎯 Gitko AI Agent 사용 가능한 명령어:
- /review: 코드 리뷰 (Lubit)
- /improve: 코드 개선 (Sian)
- /parallel: 병렬 실행
- /check: 환경 확인
```

#### `/review` - 코드 리뷰

```
@gitko /review

또는

@gitko 이 파일을 리뷰해줘
```

**결과**:
```
🛡️ Lubit 코드 리뷰

✅ 리뷰 완료
- 코드 품질: 양호
- 보안: 주의사항 2개
- 성능: 최적화 가능 영역 1개
```

#### `/improve` - 코드 개선

```
@gitko /improve

또는

@gitko 이 코드를 개선해줘
```

**결과**:
```
🔧 Sian 코드 개선

✅ 개선 완료
- 함수 분리: 3개
- 중복 제거: 5곳
- 가독성: 향상
```

#### `/parallel` - 병렬 실행

```
@gitko /parallel

또는

@gitko 종합 분석해줘
```

**결과**:
```
🔄 Sian + Lubit 동시 실행 중...

✅ Sian 결과: 개선사항 12개
✅ Lubit 결과: 주의사항 3개
```

#### `/check` - 환경 확인

```
@gitko /check
```

**결과**:
```
🔍 환경 설정 확인

## Python 환경
- Python: ✅ D:/nas_backup/LLM_Unified/.venv/Scripts/python.exe
- Script: ✅ D:/nas_backup/LLM_Unified/ion-mentoring/gitko_cli.py

## Extension 상태
- 버전: 0.3.1
- Agent: 3개 활성화
```

---

## 📊 모니터링 대시보드

### 1. Task Queue Monitor

**실행**: `Ctrl+Shift+P` → `Gitko: Show Task Queue Monitor`

**기능**:
- 실시간 작업 큐 상태
- 대기/실행/완료 작업 확인
- 작업 성공률 통계

**사용 시나리오**:
- HTTP 폴러 작동 확인
- 작업 처리 속도 모니터링
- 에러 발생 감지

---

### 2. Performance Monitor

**실행**: `Ctrl+Shift+P` → `Gitko: Show Performance Monitor`

**기능**:
- Agent 실행 시간 추적
- 성공/실패율 통계
- 평균 응답 시간

**실시간 데이터**:
```
executeAgent: 15회 실행, 평균 2.3초, 성공률 93%
scan_screen: 8회, 평균 1.1초, 성공률 100%
click_ui: 12회, 평균 0.5초, 성공률 92%
```

---

### 3. Activity Tracker

**실행**: `Ctrl+Shift+P` → `Gitko: Show Activity Tracker`

**기능**:
- 사용자 활동 기록
- 명령어 실행 통계
- 세션 시간 추적

**대시보드**:
```
📊 Session: 45분 | Commands: 23 | Agents: 12 | Errors: 1

Top Commands:
1. gitko.showPerformanceViewer - 5회
2. gitko.showActivityViewer - 3회

Recent Events:
- 14:32:15 [command] gitko.showActivityViewer
- 14:30:42 [agent] sian (1,234ms)
```

---

### 4. Resonance Ledger Viewer

**실행**: `Ctrl+Shift+P` → `Gitko: Show Resonance Ledger`

**기능**:
- 이벤트 타임라인
- Agent별 필터링
- Resonance Score 시각화

---

## 🛠️ 개발자 도구

### Health Check

**실행**: `Ctrl+Shift+P` → `Gitko Dev: Health Check`

**결과**:
```
=== Gitko Extension Health Check ===

Status: ✅ Healthy

Memory:
  RSS: 45.2 MB
  Heap: 23.1 / 28.5 MB

Performance:
  ✅ executeAgent: 평균 2.1초
  ✅ scan_screen: 평균 1.0초
```

---

### Export Diagnostics

**실행**: `Ctrl+Shift+P` → `Gitko Dev: Export Diagnostics`

**생성 파일**: `gitko-diagnostics-2025-11-14T15-30-45.md`

**내용**:
- 시스템 정보
- 메모리 사용량
- 성능 요약
- 설정 덤프

---

### Memory Stats

**실행**: `Ctrl+Shift+P` → `Gitko Dev: Show Memory Stats`

**출력**:
```
Memory: RSS=45.2 MB, Heap=23.1/28.5 MB
```

---

## 🎬 실전 시나리오

### 시나리오 1: 레거시 코드 현대화

```
1. 파일 열기: old-legacy.js

2. Copilot Chat:
   "이 코드를 ES6+로 현대화하고 타입도 추가해줘"

3. Sian 자동 실행:
   - var → const/let
   - 함수 → 화살표 함수
   - JSDoc → TypeScript 타입

4. Lubit 자동 검증:
   - 타입 안전성 확인
   - 잠재적 버그 탐지

5. 결과: modern-code.ts 생성
```

---

### 시나리오 2: 보안 취약점 스캔

```
1. 프로젝트 전체 선택

2. @gitko /review
   "보안 취약점 전체 스캔해줘"

3. Lubit 실행:
   📊 총 127개 파일 스캔
   
4. 결과:
   ⚠️ SQL Injection: 3곳
   ⚠️ XSS: 2곳
   ⚠️ 하드코딩된 시크릿: 1곳
   
5. 수정 제안:
   - Prepared Statement 사용
   - HTML Escape 추가
   - 환경변수로 이동
```

---

### 시나리오 3: 성능 최적화

```
1. 느린 함수 발견

2. Copilot Chat:
   "이 함수가 느린데 최적화 방법 알려줘"

3. Sian 분석:
   현재: O(n²) - 100ms
   개선: O(n) - 5ms
   
4. 제안:
   - Map 사용으로 조회 최적화
   - 중복 계산 제거
   - 메모이제이션 적용

5. Performance Monitor로 확인:
   ✅ 95% 성능 향상
```

---

### 시나리오 4: 프로젝트 전체 리팩토링

```
1. @gitko /parallel
   "프로젝트 전체를 개선해줘"

2. Gitko Orchestrator 실행:
   🎭 Sian + Lubit 동시 작업
   
3. 진행 상황:
   [Sian] 파일 1/50... 개선사항 3개
   [Lubit] 파일 1/50... 이슈 1개
   
4. 최종 보고서:
   📊 Sian: 총 45개 개선사항
   🛡️ Lubit: 총 8개 보안 이슈
   
5. Activity Tracker 확인:
   - 작업 시간: 15분
   - 처리된 파일: 50개
```

---

## 📝 팁 & 트릭

### 1. 자연어로 말하기
❌ 나쁜 예:
```
@gitko /review --security --performance
```

✅ 좋은 예:
```
이 코드에 보안 문제와 성능 이슈가 있는지 확인해줘
```

### 2. 컨텍스트 제공
❌ 나쁜 예:
```
개선해줘
```

✅ 좋은 예:
```
이 React 컴포넌트가 리렌더링이 너무 많아. 
성능을 개선하고 불필요한 렌더링을 줄여줘
```

### 3. 모니터링 활용
```
작업 전: Performance Monitor 열기
작업 후: Activity Tracker로 확인
문제 발생: Health Check 실행
```

### 4. 단계별 작업
```
1단계: Lubit으로 현재 상태 분석
2단계: Sian으로 개선안 도출
3단계: Gitko로 종합 실행
```

---

## 🚨 문제 해결

### Agent가 응답 없음

1. Output Channel 확인
   ```
   View → Output → "Gitko Extension"
   ```

2. Python 경로 확인
   ```
   @gitko /check
   ```

3. 로그 확인
   ```
   [에러 메시지에서 원인 파악]
   ```

### 성능이 느림

1. Performance Monitor 확인
   ```
   Ctrl+Shift+P → "Gitko: Show Performance Monitor"
   ```

2. Health Check 실행
   ```
   Ctrl+Shift+P → "Gitko Dev: Health Check"
   ```

3. 메모리 확인
   ```
   ⚠️ Heap > 80%: 재시작 권장
   ```

---

## 📚 추가 자료

- [Quick Start](QUICKSTART.md) - 5분 시작 가이드
- [Setup Guide](SETUP_GUIDE.md) - 상세 설정
- [Release Notes v0.3.1](RELEASE_NOTES_v0.3.1.md) - 최신 기능
- [Final Summary](FINAL_SUMMARY.md) - 전체 요약

---

**Happy Coding! 🚀**

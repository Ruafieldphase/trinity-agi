#!/usr/bin/env bash
# SESSION_BOOTSTRAP_2025-10-23_v1_5_PREVIEW.yaml
export LUMEN_SESSION="v1.5_Preview"
export LUMEN_V15="$PWD/lumen_v1_5_preview_assets"

l15.ingest() { (cd "$LUMEN_V15" && python streaming_ingestor_v15.py); }
l15.maturity() { (cd "$LUMEN_V15" && python maturity_index_v15.py); }
l15.graph() { (cd "$LUMEN_V15" && python unified_feedback_graph_v15.py); }

l15.run() { l15.ingest && l15.maturity && l15.graph; }
echo "[Lumen] v1.5 preview bootstrapped. Try: l15.run"

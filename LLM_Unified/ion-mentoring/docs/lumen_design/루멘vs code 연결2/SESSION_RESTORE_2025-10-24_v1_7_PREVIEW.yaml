#!/usr/bin/env bash
# Lumen — Session Restore (v1.7 Preview)
export LUMEN_SESSION="v1.7_Preview"
export LUMEN_ROOT="${LUMEN_ROOT:-$PWD}"
export LUMEN_V17="${LUMEN_V17:-$LUMEN_ROOT/lumen_v1_7_assets}"

l7.build()  { (cd "$LUMEN_V17" && python feedback_graph_core_v17.py); }
l7.export() { (cd "$LUMEN_V17" && python metrics_feedback_graph_exporter.py); }
l7.smoke()  { (cd "$LUMEN_V17" && make smoke); }

echo "[Lumen] v1.7 Preview session restored. Try: l7.smoke"

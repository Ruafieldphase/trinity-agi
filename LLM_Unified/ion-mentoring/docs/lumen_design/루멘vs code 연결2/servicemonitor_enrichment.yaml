apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lumen-enrichment
  labels: { release: prometheus }
spec:
  selector:
    matchLabels:
      app: lumen
  namespaceSelector: { any: true }
  endpoints:
    - port: enrichment
      interval: 10s

# 긴급 롤백 훈련 가이드

**작성일**: 2025-10-21  
**목적**: 비상 상황 대응 능력 확보  
**소요 시간**: 15-20분  
**난이도**: ⭐⭐☆☆☆ (중급)

---

## 🎯 훈련 목표

1. 긴급 롤백 프로세스 숙지
2. Dry-Run 모드 사용법 이해
3. 실전 상황 시뮬레이션
4. 의사결정 연습 (롤백 vs 관찰)

---

## 📋 사전 준비

### 필수 확인 사항

- [ ] PowerShell 7+ 또는 Windows PowerShell 5.1
- [ ] GCP 프로젝트 접근 권한 (`naeda-genesis`)
- [ ] `gcloud` CLI 설치 및 인증 완료
- [ ] VS Code 또는 터미널 준비

### 디렉토리 이동

```powershell
cd d:\nas_backup\LLM_Unified\ion-mentoring\scripts
```

---

## 🎓 훈련 시나리오

### 시나리오 1: 정상 상태에서 Dry-Run

**상황**: 시스템이 정상이지만 롤백 프로세스를 연습하고 싶다

#### 실행 명령

```powershell
.\emergency_rollback.ps1 -DryRun
```

#### 예상 출력

```
[emergency-rollback] ==========================================
[emergency-rollback] EMERGENCY ROLLBACK - DRY RUN MODE
[emergency-rollback] ==========================================

[emergency-rollback] Stage 1/4: Checking current health status...
  Status: HEALTHY
  Error Rate: 0%
  P95 Latency: 49.74ms
  Decision: System is healthy, rollback not needed

[emergency-rollback] DRY RUN - Would skip rollback (system healthy)
[emergency-rollback] Use -Force to rollback even when healthy

[emergency-rollback] ==========================================
[emergency-rollback] DRY RUN COMPLETE - No changes made
[emergency-rollback] ==========================================
```

#### 학습 포인트

- ✅ 정상 상태에서는 롤백하지 않음 (안전장치)
- ✅ `-Force` 옵션으로 강제 실행 가능
- ✅ Dry-Run은 실제 변경 없음

---

### 시나리오 2: 강제 롤백 Dry-Run

**상황**: 정상 상태지만 롤백 프로세스 전체를 시뮬레이션하고 싶다

#### 실행 명령

```powershell
.\emergency_rollback.ps1 -Force -DryRun
```

#### 예상 출력

```
[emergency-rollback] ==========================================
[emergency-rollback] EMERGENCY ROLLBACK - DRY RUN MODE
[emergency-rollback] FORCE MODE: Will rollback regardless of health
[emergency-rollback] ==========================================

[emergency-rollback] Stage 1/4: Checking current health status...
  Status: HEALTHY
  Error Rate: 0%
  P95 Latency: 49.74ms
  Force mode: Proceeding with rollback anyway

[emergency-rollback] Stage 2/4: Executing rollback...
  DRY RUN - Would execute:
    rollback_phase4_canary.ps1 -ProjectId naeda-genesis -AutoApprove
  Expected action: Canary → 0%, Legacy → 100%

[emergency-rollback] Stage 3/4: Waiting for traffic stabilization...
  DRY RUN - Would wait 30 seconds

[emergency-rollback] Stage 4/4: Verifying post-rollback status...
  DRY RUN - Would check health after rollback

[emergency-rollback] ==========================================
[emergency-rollback] DRY RUN COMPLETE
[emergency-rollback] ==========================================

Next steps (if this were real):
  1. Monitor Legacy service logs
  2. Confirm error rates drop
  3. Review canary deployment issues
  4. Plan hotfix or revert
```

#### 학습 포인트

- ✅ 4단계 프로세스 전체 시뮬레이션
- ✅ 실제 명령어 미리보기
- ✅ 예상 결과 확인

---

### 시나리오 3: 대화형 롤백 (확인 포함)

**상황**: 문제가 있어 실제로 롤백해야 하지만, 한 번 더 확인하고 싶다

#### 실행 명령

```powershell
.\emergency_rollback.ps1
# 또는
.\emergency_rollback.ps1 -Force
```

#### 예상 상호작용

```
[emergency-rollback] ==========================================
[emergency-rollback] EMERGENCY ROLLBACK
[emergency-rollback] ==========================================

[emergency-rollback] Stage 1/4: Checking current health status...
  Status: UNHEALTHY
  Error Rate: 1.2% (threshold: 0.5%)
  P95 Latency: 350ms (threshold: 200ms)
  Decision: Rollback recommended

[emergency-rollback] WARNING: This will rollback Canary to 0% traffic
[emergency-rollback] Are you sure you want to proceed? (y/n): _
```

#### 응답 옵션

1. **y (Yes)**: 롤백 진행
2. **n (No)**: 취소하고 종료

#### 학습 포인트

- ✅ 최종 확인 단계 (실수 방지)
- ✅ 헬스 상태 기반 의사결정
- ✅ 언제든지 취소 가능

---

### 시나리오 4: 즉시 롤백 (확인 생략)

**상황**: 심각한 장애 발생, 즉시 롤백 필요

#### 실행 명령

```powershell
.\emergency_rollback.ps1 -Force -SkipConfirmation
```

#### 예상 출력

```
[emergency-rollback] ==========================================
[emergency-rollback] EMERGENCY ROLLBACK - IMMEDIATE MODE
[emergency-rollback] ==========================================

[emergency-rollback] Stage 1/4: Checking current health status...
  Status: UNHEALTHY
  Error Rate: 5.8% (threshold: 0.5%)
  P95 Latency: 1250ms (threshold: 200ms)

[emergency-rollback] Stage 2/4: Executing rollback...
  Executing: rollback_phase4_canary.ps1 -ProjectId naeda-genesis -AutoApprove
  [rollback] Setting Canary traffic to 0%...
  [rollback] Setting Legacy traffic to 100%...
  ✓ Rollback complete

[emergency-rollback] Stage 3/4: Waiting for traffic stabilization...
  Waiting 30 seconds...
  ✓ Complete

[emergency-rollback] Stage 4/4: Verifying post-rollback status...
  Checking health...
  Status: HEALTHY
  Error Rate: 0.1%
  P95 Latency: 85ms
  ✓ System recovered

[emergency-rollback] ==========================================
[emergency-rollback] ROLLBACK SUCCESSFUL
[emergency-rollback] ==========================================

Next steps:
  1. Monitor Legacy service for 10 minutes
  2. Review Canary deployment logs
  3. Identify root cause
  4. Plan hotfix or redeploy
```

#### 학습 포인트

- ✅ 가장 빠른 롤백 방법 (< 2분)
- ✅ 확인 절차 생략 (비상용)
- ✅ 자동 검증까지 포함

---

## 🧪 실습 과제

### 과제 1: Dry-Run 실행 (5분)

**목표**: 안전하게 롤백 프로세스 확인

```powershell
# 1. Dry-Run 실행
.\emergency_rollback.ps1 -Force -DryRun

# 2. 출력 내용 확인
#    - 4단계 프로세스
#    - 예상 명령어
#    - "DRY RUN COMPLETE" 메시지
```

**성공 기준**:

- [ ] "DRY RUN MODE" 표시 확인
- [ ] 4단계 모두 출력됨
- [ ] "No changes made" 확인
- [ ] 실제 트래픽 변화 없음

---

### 과제 2: VS Code Task로 실행 (3분)

**목표**: UI에서 원클릭 실행

1. VS Code에서 `Ctrl+Shift+P` (또는 `Cmd+Shift+P`)
2. "Tasks: Run Task" 입력
3. "Emergency: Rollback Dry-Run (Training)" 선택
4. 출력 확인

**성공 기준**:

- [ ] Task 목록에서 찾기 성공
- [ ] 터미널 자동 실행
- [ ] Dry-Run 출력 확인

---

### 과제 3: 헬스 상태 확인 후 판단 (5분)

**목표**: 현재 상태 분석 및 의사결정 연습

```powershell
# 1. 현재 헬스 확인
.\check_monitoring_status.ps1

# 2. 결과 해석
#    - Error Rate가 임계치 초과? → 롤백 고려
#    - P95 Latency가 임계치 초과? → 롤백 고려
#    - 둘 다 정상? → 관찰 지속

# 3. 의사결정
if (에러율 > 0.5% 또는 P95 > 200ms) {
    # Dry-Run으로 연습
    .\emergency_rollback.ps1 -Force -DryRun
}
```

**판단 기준**:
| 조건 | Error Rate | P95 Latency | 조치 |
|------|-----------|-------------|------|
| 정상 | ≤ 0.5% | ≤ 200ms | 관찰 지속 |
| 경미 | 0.5% - 1% | 200ms - 500ms | 강화 모니터링 (10분 간격) |
| 심각 | > 1% | > 500ms | **즉시 롤백** |

---

### 과제 4: 실전 시뮬레이션 (10분)

**목표**: 전체 프로세스 체득

**시나리오**: 오후 2시, Slack에 알림 도착 - "P95 레이턴시 급증!"

#### 타임라인

```
14:00:00 - 알림 수신
14:00:30 - 헬스체크 수동 실행
14:01:00 - 상황 평가 (P95 = 450ms, Error = 0.8%)
14:01:30 - 롤백 결정
14:02:00 - 롤백 실행 (Dry-Run으로 연습)
14:02:30 - (실전이라면) 실제 롤백
14:03:00 - 대기 30초
14:03:30 - 헬스 재확인
14:04:00 - 상황 종료 보고
```

#### 실행 스크립트 (Dry-Run)

```powershell
Write-Host "`n[14:00:30] Step 1: Health Check" -ForegroundColor Yellow
.\check_monitoring_status.ps1

Write-Host "`n[14:01:00] Step 2: Decision" -ForegroundColor Yellow
Write-Host "  P95 = 450ms (threshold: 200ms) → CRITICAL"
Write-Host "  Error = 0.8% (threshold: 0.5%) → WARNING"
Write-Host "  Decision: ROLLBACK IMMEDIATELY"

Write-Host "`n[14:01:30] Step 3: Execute Rollback (Dry-Run)" -ForegroundColor Yellow
.\emergency_rollback.ps1 -Force -DryRun

Write-Host "`n[14:04:00] Step 4: Post-Rollback Report" -ForegroundColor Yellow
Write-Host "  ✓ Rollback completed (Dry-Run)"
Write-Host "  ✓ Legacy at 100% traffic"
Write-Host "  ✓ Canary at 0% traffic"
Write-Host "  ✓ Monitoring for 10 minutes"
```

**성공 기준**:

- [ ] 전체 과정 5분 이내 완료 (Dry-Run 기준)
- [ ] 각 단계 출력 확인
- [ ] 의사결정 논리 이해
- [ ] 실전 자신감 획득

---

## 📊 훈련 결과 체크리스트

### 기본 역량

- [ ] `emergency_rollback.ps1` 위치 알고 있음
- [ ] Dry-Run 모드 실행 가능
- [ ] `-Force` 옵션 이해
- [ ] `-SkipConfirmation` 옵션 이해

### 중급 역량

- [ ] 헬스체크 결과 해석 가능
- [ ] 롤백 필요성 판단 가능
- [ ] 4단계 프로세스 설명 가능
- [ ] VS Code Task로 실행 가능

### 고급 역량

- [ ] 전체 타임라인 5분 내 실행
- [ ] 에스컬레이션 시점 판단 가능
- [ ] 포스트모템 작성 준비 완료
- [ ] 팀원 교육 가능

---

## 🆘 자주 묻는 질문 (FAQ)

### Q1: Dry-Run에서 실제 실행으로 전환하는 방법?

**A**: Dry-Run 결과가 예상과 같으면, `-DryRun` 옵션을 제거하고 재실행

```powershell
# Dry-Run으로 확인
.\emergency_rollback.ps1 -Force -DryRun

# 확인 후 실제 실행
.\emergency_rollback.ps1 -Force -SkipConfirmation
```

### Q2: 롤백 후 다시 Canary로 전환하는 방법?

**A**: `deploy_phase4_canary.ps1` 스크립트 사용

```powershell
# 5%부터 다시 시작 (안전)
.\deploy_phase4_canary.ps1 -ProjectId naeda-genesis -CanaryPercentage 5

# 또는 이전 상태로 (예: 100%)
.\deploy_phase4_canary.ps1 -ProjectId naeda-genesis -CanaryPercentage 100
```

### Q3: 롤백 중에 취소할 수 있나요?

**A**:

- **대화형 모드**: Yes/No 프롬프트에서 `n` 입력으로 취소 가능
- **즉시 모드** (`-SkipConfirmation`): 시작되면 취소 불가 (30초 이내 완료)

### Q4: 롤백 실패 시 어떻게 하나요?

**A**:

1. 즉시 Level 2 에스컬레이션 (온콜 엔지니어)
2. GCP 콘솔에서 수동 트래픽 조정
3. 에러 로그 수집 (`emergency_rollback_*.log`)
4. 포스트모템 준비

### Q5: 훈련 중 실수로 실제 실행한 경우?

**A**:

1. 당황하지 말 것! (30초 내 완료)
2. 즉시 헬스체크 실행: `.\check_monitoring_status.ps1`
3. 정상이면 다시 Canary로: `.\deploy_phase4_canary.ps1 -CanaryPercentage 100`
4. 사건 기록 (학습 자료)

---

## 🎖️ 인증서

훈련 완료 후 아래 항목을 팀 리더에게 보고:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
         긴급 롤백 훈련 완료 인증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  이름: ___________________
  날짜: 2025-10-21

  완료 과제:
  [✓] 과제 1: Dry-Run 실행
  [✓] 과제 2: VS Code Task 실행
  [✓] 과제 3: 헬스 상태 판단
  [✓] 과제 4: 실전 시뮬레이션

  소요 시간: _____ 분
  자신감 수준 (1-5): _____

  서명: ___________________
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 📚 다음 단계

### 개인 학습

1. **포스트모템 작성 연습**: 가상 장애 시나리오로 보고서 작성
2. **모니터링 강화**: 일일 리포트 분석 숙달
3. **자동화 이해**: `auto_remediation.ps1` 코드 리뷰

### 팀 활동

1. **분기별 훈련**: 3개월마다 전체 팀 롤백 훈련
2. **온콜 로테이션**: 긴급 대응 경험 쌓기
3. **Chaos Engineering**: 의도적 장애 주입 테스트

---

**작성자**: 깃코 (Autonomous Operations Agent)  
**최종 업데이트**: 2025-10-21  
**버전**: 1.0

**연락처**: 질문이나 피드백은 팀 Slack `#phase4-ops` 채널로
